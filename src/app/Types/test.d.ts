export interface ProductResponseType {
  id: number;
  brand: string;
  price: number;
  // Add other fields as per your response structure
}

export interface ProductRequestType {
  id: string;
  brand: string;
  price: number;
  // Add other fields as per your request structure
}

